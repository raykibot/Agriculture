<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luo.share.mapper.ChatMessageMapper">

    <insert id="insertMessage">
        INSERT INTO chat_messages
            (id, from_user_id, to_user_id, content, is_read, create_time)
        VALUES
            (#{id}, #{fromUserId}, #{toUserId}, #{content}, #{isRead}, NOW())
    </insert>

    <select id="selectHistory" resultType="com.luo.share.model.entity.ChatMessage">
        SELECT
            id, from_user_id, to_user_id, content, is_read, create_time
        FROM
            chat_messages
        WHERE
            (from_user_id = #{userId1} AND to_user_id = #{userId2})
           OR
            (from_user_id = #{userId2} AND to_user_id = #{userId1})
        ORDER BY
            create_time ASC
    </select>

</mapper>